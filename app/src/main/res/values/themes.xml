<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      A bare splash theme: the browser paints its own surfaces from Compose, so the platform
      theme only has to avoid a flash of the wrong colour on launch.
    -->
    <style name="Theme.Slate" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/launch_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.Slate.Main</item>
    </style>

    <style name="Theme.Slate.Main" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/launch_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
